{
  "name": "xshell-agent-bridge",
  "version": "0.4.0",
  "private": true,
  "description": "Safe MCP bridge for Xshell terminal control and OpenSSH file transfers with local user approval.",
  "type": "module",
  "scripts": {
    "start": "node src/daemon.mjs",
    "mcp": "node src/mcp-stdio.mjs",
    "demo:bridge": "node scripts/mock-xshell-bridge.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
